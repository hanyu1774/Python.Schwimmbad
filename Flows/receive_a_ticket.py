class ReciveATicket:
    def Run(cls, given_tickets, max_capacity) -> int:
       # is_max_capacity_value_zero_or_null = (max_capacity == 0) or (max_capacity is None)
       # is_given_tickets_value_zero_or_null = (given_tickets == 0) or (given_tickets is None)
       # was_capacity_reached = given_tickets <= max_capacity

        #if not (is_max_capacity_value_zero_or_null and is_given_tickets_value_zero_or_null) and not was_capacity_reached:
        #    print("A customer receives a ticket!")
        #    return 1
        
       # elif not (is_max_capacity_value_zero_or_null and is_given_tickets_value_zero_or_null) and was_capacity_reached:
        #    print("Capacity of public swimming pool has been reached!")
         #   print("As a result, further customers can't get their tickets!")
          #  return 0;
        return 0;
